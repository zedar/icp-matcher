"""
Convert sample data to JSON.
"""
import json
import numpy as np

"""
Convert matrix of elements to features in JSON.
Args:
  X: input matrix of elements. First 3 columns = xyz coordinates, 4th column = category
Returns:
  JSON with features
"""
def to_json(X):
  pass